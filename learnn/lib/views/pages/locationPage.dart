import 'package:flutter/material.dart';

class locationPage extends StatelessWidget {
  const locationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("This is Location Page"));
  }
}
